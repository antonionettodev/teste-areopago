import { CollectionConfig } from 'payload/types';

const Minutes: CollectionConfig = {
  slug: 'minutes',
  fields: [
    {
      name: 'title',
      type: 'text'
    }
  ],
}

export default Minutes